#include <bits/stdc++.h>
using namespace std;

// https://open.kattis.com/problems/interviewqueue

void solve (queue<int> &que){
    int cont = 0;
    bool flag = true;
    while (1){
        if(flag == true)
            break;
    }
    
}

int main(){
    int N, x;
    cin >> N;
    queue<int> que;
    while (N--){
        cin >> x;
        que.push(x);        
    }
    solve(que);
}

