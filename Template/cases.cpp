#include <bits/stdc++.h>
using namespace std;

void run() {
  
}

int main(int, char**) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int T;
  scanf("%d", &T);
  for (int i = 1; i <= T; ++i) {
    cout << "Case #" << i << ": "; run();
  }

  return 0;
}