#include <bits/stdc++.h>
using namespace std;

void run() {
  int n, d, c ,m;
  cin >> n >> d >> c >> m;

  char str[n];
  cin >> str;

  


  
}

int main(int, char**) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int T;
  scanf("%d", &T);
  for (int i = 1; i <= T; ++i) {
    cout << "Case #" << i << ": "; run();
  }

  return 0;
}