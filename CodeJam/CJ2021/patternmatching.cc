#include <bits/stdc++.h>

int main(int, char**) {
  using namespace std;
  ios_base::sync_with_stdio(false); cin.tie(nullptr);
  int T;
  cin >> T;
  for (int i = 1; i <= T; ++i) {
    int N; 
    cin >> N;
    string F[N]; 
    for (string& v : F) {
      cin >> v;
    }
    string ST, MD, FN;
    for (string state : F) {

    }


    printf("Case #%d: %d\n", i, i);
  }

  return 0;
}