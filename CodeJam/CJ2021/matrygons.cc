#include <iostream>
using namespace std;

uint64_t arr[1000000];

uint64_t function() {
    

	return 0;
}

int main(int, char**) {
  ios_base::sync_with_stdio(false); cin.tie(nullptr);
  int T;
  cin >> T;
  for (int i = 1; i <= T; ++i) {
    printf("Case #%d: %lld\n", i, function()); 
  }

  return 0;
}